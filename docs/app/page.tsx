// Placeholder landing route. The real landing page at `/` is deferred
// (see issue #244). This stub keeps `next build` green until commit 2
// wires up the `/docs` route group.
export default function HomePage() {
  return (
    <main>
      <h1>Controller</h1>
      <p>Documentation landing page coming soon.</p>
    </main>
  );
}
